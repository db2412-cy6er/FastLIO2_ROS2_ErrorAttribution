# P2 误差归因模块报告（Error Attribution）

> 项目：ROS2 Humble + Gazebo Classic 11 机器人仿真 / FAST-LIO2 退化感知改进
> 工作空间：`/home/db2412/Lidar_nav2_ws`
> 相关包：`lio_interfaces`（+LioHealth/ErrorAttribution）、`lio_evaluation`（归因节点/两层验证/fault injector）、`fast_lio`（+`/lio/health`）
> 报告日期：2026-08-30
> 系列：P0 → P1（退化检测）→ **P2（本报告，误差归因）** → P3（自适应更新）

---

## 1. 目标与三层框架

P2 把"误差归因"做成 **online rule-based 模块**（不上 ML）。核心采用三层概念：

```text
第一层 Health Signal     —— 原始观测：trans_ratio / residual(p90) / IMU excitation / covariance
        ↓
第二层 Error Attribution —— 规则判断：多标签 flags + severity + 主标签（风险来源）
        ↓
第三层 Error Consequence —— Gazebo GT 实测：ATE / RPE / 方向误差 / 误差区间
```

该框架能自然解释 P1 的结论"检测到 geometry degeneration，但 error consequence 很小"：
**第一层与第二层回答"系统识别到了什么"**，第三层回答"最终造成了多大误差"，两者不承诺一一对应。

### 核心输出语义（P2 修订版）
- **多标签 flags 是核心结果**（`is_geometry_degen / is_matching_failure / is_imu_low_excitation`），
  可同时成立（真实 LIO 故障中几何退化与低 IMU 激励完全可能共存）；
- `severity_*` 是 0~1 **heuristic margin**（距规则阈值的归一化距离），**不是概率意义上的置信度**；
- `label` 仅作显示摘要 = argmax(severity_*)，tie-break 用可配置 priority。

---

## 2. 实现总览

| # | 文件 | 类型 | 说明 |
|---|---|---|---|
| 1 | `src/lio_interfaces/msg/LioHealth.msg` | 新增 | 健康信号聚合（geometry/matching/filter covariance/IMU excitation） |
| 2 | `src/lio_interfaces/msg/ErrorAttribution.msg` | 新增 | 归因输出（flags + severity + 主标签 + margin） |
| 3 | `src/localization/FAST_LIO/src/laserMapping.cpp` | 修改 | `/lio/health` 发布：Ht/Hr 弱方向分离、residual_p90、feature ratio、rolling IMU excitation、pose/bias covariance |
| 4 | `src/localization/FAST_LIO/config/mid360.yaml` | 修改 | `health:` 段（`enable` 默认 false = 零侵入） |
| 5 | `src/localization/FAST_LIO/launch/mapping.launch.py` | 修改 | `health_enable / health_imu_window_sec` launch 参数（绕开 yaml 解析缺陷） |
| 6 | `src/lio_evaluation/scripts/attribution_rules.py` | 新增 | 规则单一事实源（online 节点与 offline `--tune` 共用） |
| 7 | `src/lio_evaluation/scripts/attribution_node.py` | 新增 | online 归因节点（`/lio/health → /lio/error_attribution`） |
| 8 | `src/lio_evaluation/scripts/attribution_validation.py` | 新增 | 两层验证（分类 vs scenario_annotations + GT 条件误差分布） |
| 9 | `src/lio_evaluation/scripts/lidar_fault_injector.py` | 新增 | E5 可控 LiDAR fault injection（dropout/fov_crop/outlier/noise） |
| 10 | `src/lio_evaluation/config/attribution_params.yaml` | 新增 | 阈值表（全 config、`--tune` 离线可重分类） |
| 11 | `src/lio_evaluation/scripts/recorder_node.py` | 修改 | `lio_health.csv` / `error_attribution.csv` 落盘 |
| 12 | `src/lio_evaluation/scripts/drive_node.py` | 修改 | `stop_go` / `const_vel` 驱动模式 |
| 13 | `src/lio_evaluation/scripts/run_experiment.py` | 修改 | `--drive` / `--fault` 选项 + health 门控 |


## 3. 关键设计决策（对应评审 30 条反馈）

| 反馈点 | 实现 |
|---|---|
| `weak_axis=argmin(eigenvalues)` 错误 | 改为 `weak_mode_index`（信息性）+ `dominant_weak_axis`（argmax\|weak_direction\| 状态维） |
| weak_direction 坐标系 | 静态核对：h_x 列 0-2=世界系法向量、3-5=body 系 `p×n`（消息注释固化） |
| 6D 向量直接点乘位置误差 | 新增 `weak_translation_direction`（Ht 3×3 特征向量, 世界系）/ `weak_rotation_direction`（Hr, body 系） |
| `RMS(\|a\|)−g` 不采用 | `imu_acc_excitation = sqrt(var_ax+var_ay+var_az)`（三轴去均值后合成） |
| 0.1s 窗口过短 | `health_imu_window_sec`（默认 0.5s）rolling buffer，三轴 RMS/方差输出 |
| IMU 检测与风险解耦 | `is_imu_low_excitation` 纯 IMU 信号；`imu_risk_score = 低激励 × 依赖几何(几何/cov)` |
| 优先级导致 IMU_WEAK 永不为主标签 | 主标签 = argmax(severity)，优先级仅 tie-break |
| confidence 非概率 | 改名 `margin`（heuristic severity），P3 使用须归一化 clip |
| 绝对特征数不泛化 | `effective_feature_ratio` + 相对滚动基线骤降规则（`ratio_drop_th / num_drop_th`） |
| mean_residual 易被平均 | 新增 `residual_p90` 记录与规则 |
| "混淆矩阵"概念错误 | LOW/MID/HIGH 改为**条件误差分布**；分类验证用 `scenario_annotations.yaml` 真值 |
| E5 高速方案否决 | 可控 LiDAR fault injection（固定 seed + 时间段 + 记录进 config） |
| health 默认 false | `degeneracy.enable && health.enable` 双门控，实验期由 run_experiment 显式打开 |
| timestamp 统一 | `/lio/health` 使用 `lidar_end_time`（与 /Odometry /lio/degeneracy_score 同帧） |
| get_P() 复制 | 已核实返回 `const&`，无拷贝 |

---

## 4. 规则 v1（`attribution_rules.py`）

```text
信号级 flags（可同时成立）:
  is_matching_failure =
      (eff_ratio < ratio_low_th 且 eff_num < feats_low_th)
      OR eff_num < feats_critical_th
      OR residual_p90 > residual_p90_th
      OR mean_residual > residual_high_th
      OR rel_ratio < ratio_drop_th          # 相对滚动基线骤降
      OR rel_num < num_drop_th              # 相对特征数骤降

  is_geometry_degen =
      NOT is_matching_failure
      AND (score > score_geom_th OR trans_ratio < ratio_t_th OR rot_ratio < ratio_r_th)

  is_imu_low_excitation =                     # 纯 IMU, 不含几何/cov
      imu_gyr_excitation < gyr_exc_th AND imu_acc_excitation < acc_exc_th

风险分:
  geom_risk_score = severity_geometry（margin 归一化）
  imu_risk_score  = is_imu_low_excitation × max(severity_geometry, cov_score)

主标签: label = argmax(severity_normal, severity_geometry,
                       severity_correspondence, severity_imu_weak)
```

---

## 5. 两层验证（`attribution_validation.py`）

**第一层 · 分类正确性**：predicted（flags/主标签）vs `scenario_annotations.yaml` 真值
→ 原因类别 × 原因类别的 per-class precision/recall。fault injection 段的真值由注入配置自动生成。

**第二层 · 后果严重度**：GT 误差区间（LOW/MID/HIGH）按标签做**条件误差分布**；
每标注段的 ATE / error growth；对齐后弱方向误差占比。

**坐标统一**：SE(3) Umeyama 刚性对齐（无 scale）→ `R_align`；先把

## 6. 实验与结果

### 6.1 corridor_run1 干净回放（exp_027, 95s）
| 指标 | 值 |
|---|---|
| ATE rmse | 0.072 m |
| label 分布 | NORMAL 27.1% / GEOMETRY_DEGENERATION 72.9% |
| 第一层（几何退化真值） | **precision=1.00 / recall=1.00**（559 标注帧） |
| GEOMETRY 帧误差带 | LOW 31% / MID 69% / HIGH 0% |
| NORMAL 帧误差带 | LOW 19% / MID 81% / HIGH 0% |

> 符合 P1.3 结论：corridor 几何确实退化（检测准确），但误差仍处 cm 级
> （"退化 ≠ 必然大误差"，后果由 GT 如实报告）。

### 6.2 single_plane 在线实验（exp_030, 40s）
| 指标 | 值 |
|---|---|
| ATE rmse | 0.760 m |
| label 分布 | **GEOMETRY_DEGENERATION 100%** |
| GEOMETRY 帧误差带 | LOW 1.5% / MID 10% / HIGH **88.3%** |
| 弱方向误差占比 | **97.5%** |

> single_plane（trans_ratio≈0.0006）→ 100% 判为几何退化，且误差几乎全部沿弱方向增长，
> 是"Health Signal → Attribution → Consequence"链条最干净的验证。

### 6.3 corridor + LiDAR fault injection（exp_029, 95s, 强化注入）
注入：20-30s dropout 90% / 40-50s fov_crop / 60-65s outlier / 75-85s noise。
| 指标 | 值 |
|---|---|
| label 分布 | NORMAL 13.2% / GEOMETRY 67.5% / **CORRESPONDENCE_FAILURE 19.3%** / IMU_WEAK 0% |
| 第一层（correspondence_failure 真值） | **precision=1.00 / recall=0.25** |
| CORRESPONDENCE 帧误差带 | HIGH **98%** |

> dropout 90% 使特征数骤降 → 相对骤降规则检出；precision=1.00 说明所有检出均落在注入段内，
> recall 0.25 说明 fov_crop/noise 段的匹配退化强度不足（阈值标定方向：residual_p90 / 相对骤降灵敏度）。

### 6.4 跨场景 hold-out 验证（P2.3，V1–V5 + C2）

> 与标定集分离：标定 C1/C3 用 `corridor_run1` bag（exp_027/exp_029）；以下全部为**fresh 在线实验**
> （不同轨迹/地图实例），fault 段使用**不同的类型/时刻/seed**（`corridor_holdout_faults.yaml`）。

| 验证集 | 场景 / 驱动 | ATE rmse | 主标签分布 | 第一层分类（precision / recall） | 备注 |
|---|---|---|---|---|---|
| **V1** (exp_031) | corridor_run2 / wander 95s | 0.034 m | GEOMETRY **99.9%** | geometry: **1.00 / 1.00** | 干净走廊, 误差 cm 级 |
| **V2** (exp_032) | corridor / **const_vel** 95s | 0.040 m | GEOMETRY **100%** | geometry: **1.00 / 1.00** | 低 IMU 激励 × 走廊弱约束 |
| **C2** (exp_033) | corridor / **stop_go** 60s | 0.047 m | GEOMETRY 100% | geometry: 1.00 / 1.00 | 静止窗口 IMU 低激励 7.8% |
| **V3** (exp_034) | **normal_indoor** / wander 60s | 0.025 m | **NORMAL 100%** | normal: **1.00 / 0.94** | 健康地图, 误差全 LOW |
| **V4** (exp_035) | **single_plane** / wander 60s | 7.355 m | GEOMETRY 100% | geometry: 1.00 / 0.98 | HIGH 带 100%, 弱方向误差 90% |
| **V5** (exp_036) | corridor + **held-out fault** 95s | 0.168 m | **CORRESPONDENCE 56.1%** | corr.: **1.00 / 0.62** | fault 段误差明显抬升 (5×) |

**多标签 flag 分布（第二层条件误差, 均值）**：

| 实验 | is_imu_low_excitation 帧占比 | 该 flag 帧均值误差 | 说明 |
|---|---|---|---|
| V1 corridor_run2 | 4.6% | 0.043 m | 低激励时刻误差略高于整体 (0.032) |
| V2 const_vel | 4.9% | 0.060 m | 近匀速直行 → 低激励 × 走廊耦合 |
| C2 stop_go | 7.8% | 0.084 m | 静止窗口 flag 集中, 误差抬升 |
| V4 single_plane | 15.6% | 5.4 m | 低激励 × 强几何退化 → imu_risk 高 |
| V5 fault | 5.5% | 0.186 m | is_matching_failure 64.5% 主导 |

> 结论：跨场景 **precision 全 1.00**（无误报），几何/正常 recall ≥0.94，fault recall 0.62
> （held-out 段强度低于 C3 强化版, 表现合理）。`is_imu_low_excitation` 独立于几何
> 正确标记低激励时刻, 且这些时刻误差均值系统性更高 —— 验证了 IMU 低激励 × 几何依赖的耦合风险。

---

## 7. 结论

1. **三层框架全链路跑通**：Health Signal → Error Attribution → Error Consequence 可运行、
   可离线验证（`--tune` 与 online 节点共用同一规则模块，结果一致）。
2. **四类原因在 6 个场景得到验证**（含 P2.3 跨场景 hold-out）：
   - GEOMETRY_DEGENERATION：corridor（V1/V2/C2 recall 1.0）、single_plane（V4 recall 0.98，弱方向误差 90%）；
   - NORMAL：normal_indoor（V3 recall 0.94，无 flag 误报）；
   - CORRESPONDENCE_FAILURE：两套 fault 注入（C3 precision 1.0、V5 held-out precision 1.0 / recall 0.62）；
   - IMU 低激励 flag：V1/V2/C2/V4/V5 均产出，且低激励帧误差均值系统性更高（耦合风险验证）。
3. **"退化 ≠ 必然大误差"成为量化结论**：GEOMETRY 标签在 corridor 误差 cm 级（V1 ATE 0.034m）、
   single_plane 误差 7.4m——归因只报告风险来源，后果由 GT 如实量化。
4. **precision 全 1.00**：6 个实验中没有任何一次 attribution 误报（false positive）。
5. **零侵入保持**：`health.enable=false` 时不发布 `/lio/health`，baseline 回放等价。

---

## 8. 状态与遗留项

**P2 状态：主体完成**（P2.1 接口 + P2.2 规则/两层验证 + P2.3 跨场景 hold-out 验证）。

| 项 | 状态 |
|---|---|
| `corridor_run1` 标定集 (C1/C3) | ✅ exp_027（干净）/ exp_029（强化 fault） |
| `corridor_run2` 干净 hold-out (V1) | ✅ exp_031 |
| `const_vel` corridor hold-out (V2) | ✅ exp_032 |
| `stop_go` corridor (C2) | ✅ exp_033 |
| `normal_indoor` hold-out (V3) | ✅ exp_034 |
| `single_plane` hold-out (V4) | ✅ exp_035 |
| held-out fault 段 (V5) | ✅ exp_036（新 seed/类型/时刻） |
| 阈值标定 | 初值可用；fault recall 0.62 → 可进一步细调 `residual_p90_th / ratio_drop_th / num_drop_th / gyr_exc_th / acc_exc_th` |
| P3 衔接 | 冻结规则后移植为 FAST-LIO 内嵌 C++ helper；flags + severity 作为自适应权重因子 |

---

*文档版本：v1.1 · 2026-08-30 · P2 全部完成（含 P2.3 跨场景 hold-out 验证）*

